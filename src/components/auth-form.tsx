"use client";
import {useState}from"react";import Link from"next/link";import {useRouter}from"next/navigation";import {createUserWithEmailAndPassword,GoogleAuthProvider,sendEmailVerification,signInWithEmailAndPassword,signInWithPopup,updateProfile}from"firebase/auth";import{auth,firebaseConfigured}from"@/lib/firebase-client";import{ArrowRight,LoaderCircle}from"lucide-react";
export function AuthForm({mode}:{mode:"login"|"register"}){const router=useRouter();const[busy,setBusy]=useState(false);const[error,setError]=useState("");async function establish(user:import("firebase/auth").User){const idToken=await user.getIdToken();const res=await fetch("/api/auth/session",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({idToken})});if(!res.ok)throw new Error((await res.json()).error||"Could not start session");router.push("/select-company");router.refresh()}async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();if(!auth)return setError("Firebase is not configured. Add the environment variables first.");setBusy(true);setError("");const data=new FormData(e.currentTarget);try{const email=String(data.get("email")),password=String(data.get("password"));if(mode==="register"){const result=await createUserWithEmailAndPassword(auth,email,password);await updateProfile(result.user,{displayName:String(data.get("name"))});await sendEmailVerification(result.user);await establish(result.user)}else await establish((await signInWithEmailAndPassword(auth,email,password)).user)}catch(e){setError(e instanceof Error?e.message:"Authentication failed")}finally{setBusy(false)}}async function google(){if(!auth)return setError("Firebase is not configured.");setBusy(true);try{await establish((await signInWithPopup(auth,new GoogleAuthProvider())).user)}catch(e){setError(e instanceof Error?e.message:"Google sign-in failed")}finally{setBusy(false)}}return <form onSubmit={submit} className="space-y-4">{!firebaseConfigured&&<div className="rounded-xl border border-amber-400/40 bg-amber-400/10 p-3 text-sm">Connect Firebase in <code>.env.local</code> to enable real authentication.</div>}{mode==="register"&&<label><span className="label">Full name</span><input className="input" name="name" autoComplete="name" required/></label>}<label><span className="label">Work email</span><input className="input" type="email" name="email" autoComplete="email" required/></label><label><span className="label">Password</span><input className="input" type="password" name="password" minLength={8} autoComplete={mode==="login"?"current-password":"new-password"} required/></label>{error&&<p className="text-sm text-red-500" role="alert">{error}</p>}<button disabled={busy} className="btn btn-primary w-full">{busy?<LoaderCircle className="animate-spin" size={18}/>:<>{mode==="login"?"Sign in":"Create account"}<ArrowRight size={17}/></>}</button><div className="flex items-center gap-3 text-xs muted"><span className="h-px flex-1 bg-[var(--border)]"/>OR<span className="h-px flex-1 bg-[var(--border)]"/></div><button type="button" onClick={google} className="btn btn-secondary w-full">Continue with Google</button><p className="text-center text-sm muted">{mode==="login"?"New to Toro? ":"Already have an account? "}<Link className="font-semibold text-[var(--accent)]" href={mode==="login"?"/register":"/login"}>{mode==="login"?"Create an account":"Sign in"}</Link></p></form>}
