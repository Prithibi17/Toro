import{redirect}from"next/navigation";import Link from"next/link";import{currentUser}from"@/lib/session";export default async function Settings(){const u=await currentUser();if(!u)redirect('/login');return <main className="mx-auto min-h-screen max-w-3xl p-6 py-16"><Link href="/select-company" className="text-sm muted">← Workspaces</Link><h1 className="mt-6 text-3xl font-extrabold">Account settings</h1><div className="panel mt-7 p-6"><p className="label">Name</p><p>{u.name||'Not set'}</p><p className="label mt-5">Email</p><p>{u.email}</p><p className="mt-6 text-sm muted">Profile editing, photo upload and account deletion require Firebase Storage and re-authentication configuration.</p></div></main>}
