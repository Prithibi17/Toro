import { NextResponse } from "next/server";import{FieldValue}from"firebase-admin/firestore";import{z}from"zod";import{requireMembership}from"@/lib/session";import{getAdmin}from"@/lib/firebase-admin";import type{PermissionKey}from"@/lib/types";
const keys=["members.manage","apps.manage","tasks.create","tasks.move","tasks.assign","crm.manage","contacts.manage","sales.manage"] as const;
const input=z.object({email:z.string().email(),displayName:z.string().trim().min(2).max(100),role:z.enum(["admin","manager","employee","intern"]),departmentIds:z.array(z.string()).max(10),permissions:z.array(z.enum(keys)).max(keys.length)});
export async function POST(req:Request,{params}:{params:Promise<{companyId:string}>}){const{companyId}=await params;const ctx=await requireMembership(companyId);if(!ctx||ctx.membership.role!=="owner")return NextResponse.json({error:"Only the company owner can add members"},{status:403});try{const data=input.parse(await req.json());const{auth,db}=getAdmin();let target;try{target=await auth.getUserByEmail(data.email)}catch{}const permissions=Object.fromEntries(keys.map(k=>[k,data.permissions.includes(k)])) as Record<PermissionKey,boolean>;const company=(await db.doc(`companies/${companyId}`).get()).data()!;if(target){const memberRef=db.doc(`companies/${companyId}/members/${target.uid}`);const mirrorRef=db.doc(`users/${target.uid}/companyMemberships/${companyId}`);await db.runTransaction(async tx=>{const existing=await tx.get(memberRef);if(existing.exists)throw new Error("This user is already a company member");const membership={companyId,companyName:company.name,role:data.role,status:"active",departmentIds:data.departmentIds,permissions,enabledModules:company.enabledModules||[],createdAt:FieldValue.serverTimestamp()};tx.create(memberRef,{...membership,userId:target.uid,email:data.email,displayName:data.displayName,employmentType:data.role});tx.set(mirrorRef,membership);const note=db.collection(`companies/${companyId}/notifications`).doc();tx.create(note,{companyId,recipientId:target.uid,eventType:"company.member_added",title:`You joined ${company.name}`,message:`You were added as ${data.role}.`,read:false,createdAt:FieldValue.serverTimestamp()})});return NextResponse.json({status:"active"},{status:201})}const ref=db.collection(`companies/${companyId}/invitations`).doc();await ref.create({...data,email:data.email.toLowerCase(),permissions,status:"pending",createdBy:ctx.user.uid,createdAt:FieldValue.serverTimestamp(),expiresAt:new Date(Date.now()+7*86400000)});return NextResponse.json({status:"pending"},{status:201})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Could not add member"},{status:400})}}
