"use client";import{useState}from"react";import Link from"next/link";import{sendPasswordResetEmail}from"firebase/auth";import{auth}from"@/lib/firebase-client";export default function Forgot(){const[email,setEmail]=useState('');const[msg,setMsg]=useState('');return <main className="grid min-h-screen place-items-center p-5"><form className="panel w-full max-w-md p-7" onSubmit={async e=>{e.preventDefault();if(!auth)return setMsg('Firebase is not configured.');await sendPasswordResetEmail(auth,email);setMsg('If an account exists, Firebase has sent reset instructions.')}}><h1 className="text-2xl font-extrabold">Reset your password</h1><p className="mb-6 mt-2 text-sm muted">We’ll send a secure reset link to your email.</p><input className="input" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@company.com"/><button className="btn btn-primary mt-4 w-full">Send reset link</button>{msg&&<p className="mt-4 text-sm muted">{msg}</p>}<Link className="mt-5 block text-center text-sm font-semibold" href="/login">Back to sign in</Link></form></main>}
